# Append these settings to the existing application's environment.
# Use the actual private bridge address reachable from the API container.
HARNESS_BRIDGE_URL=http://oracle-recovery-harness:8099
# Must match the bridge token; generate a long random secret locally.
HARNESS_BRIDGE_TOKEN=
