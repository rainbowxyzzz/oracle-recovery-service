# 帮助与架构中心发布验证报告

验证日期：2026-08-20

验证环境：`192.168.150.128`
发布模式：模式二（单文件热更新，不执行回滚演练）

## 发布范围

- 新增“帮助与架构中心”主导航与只读页面。
- 新增系统总览、功能手册、接口字典、耦合关系四个视图。
- 内置 16 个业务模块的用途、操作步骤、权限、影响范围、风险和上下游知识目录。
- 从运行中 `/openapi.json` 自动发现路径、方法、参数、请求模型和响应状态。
- 接口按去除 `/api/v1` 后的最长业务路径前缀归类，避免父模块抢占子模块接口。
- 保留 `/docs` 原始 Swagger 作为开发人员补充入口。

## 安全与兼容边界

- 帮助中心仅登录用户可见，不新增业务操作权限。
- 不读取业务数据、运行请求体、日志详情、凭据、Token 或密钥。
- 不修改现有接口、数据库结构、任务、密钥、调度、离线快照或 Worker。
- 本次只热更新 API 容器内 `static/ui.html`，未重启任何容器。

## 验证结果

- 帮助中心专项测试：4 项通过。
- 项目全量回归：226 passed，1 skipped，32 subtests passed。
- 页面 JavaScript 语法检查：2 段脚本通过。
- 128 实际读取 OpenAPI 路径：184 条。
- 128 页面导航、OpenAPI 自动发现和最长前缀分类代码检查：通过。
- 128 `/api/v1/health`：HTTP 200。
- 发布前无 active/reserved Celery 任务。
- 业务计数保持不变：SM4 任务 13、密钥版本 7、调度 6、离线开发版本 82。
- API 近期日志未发现 traceback、exception、error 或 critical。

## 未自动执行项

- 本机 Chrome 插件仍被可信路径校验阻止，本轮未自动执行可见 Chrome 的点击、窗口尺寸和搜索筛选闭环；接口、静态页面、语法、自动测试及 128 运行契约已完成验证。
