# 审批流自动授权监听状态可视化发布验证

日期：2026-08-20

## 发布范围

- 模式：模式二，发布到 `192.168.150.128` 并验证。
- 仅更新 `static/ui.html`，不修改调度线程、接口、数据库结构、Worker 或部署镜像。
- 页面增加自动监听状态卡、未保存设置提示、运行记录当前监听注释和 1500px 以下单列响应式布局。

## 发布前检查

- 本地源码与 128 原运行页面 SHA256 一致。
- 最近审批监听增量包已包含原有监听开关、间隔和每轮处理数量字段。
- 审批流自动授权 `created/running` 在途运行数为 `0`。
- 内联 JavaScript 语法检查和 `git diff --check` 通过。
- 工作区 Python 运行时未安装 `pytest`，未临时改变运行环境；业务服务端逻辑未修改。

## 发布与回退点

- 最终线上页面 SHA256：`413be58b1aad020fb1633261824df1700aa5ee6f4dac44d5ccac1744dee7cab2`。
- 最终发布前备份：`/opt/oracle-recovery-hotfix-backups/approval-auth-watch-status-20260820-094659/ui.html`。
- 仅重启 `oracle-recovery-api`，所有独立 Worker 保持原容器运行。

## 真实页面闭环

- 当前配置 `dess` 服务端回读为 `auto_watch_enabled=false`、间隔 `5` 分钟、每轮 `1` 条。
- 首屏状态卡显示“监听已停用”、最近扫描和下次预计时间；3 条现有运行记录均显示“当前监听”注释。
- 勾选监听但不保存时，状态卡继续显示服务器已保存的“监听已停用”，并提示“有未保存的监听设置”。
- 恢复停用并点击“保存并应用”后，PUT 返回成功；重新加载配置后仍为停用、间隔 5 分钟，未创建自动监听运行。
- 1440、1200、960、720 四种视口均无页面级横向溢出，状态卡可见。
- 浏览器无 `pageerror`；唯一控制台 404 为站点未配置的 `/favicon.ico`，与本次功能无关。

## 发布后状态

- `/api/v1/health` 返回 `ok`，MySQL 连接正常。
- API 与 8 个独立 Worker 均为 `Up`。
- API 最近日志无 `ERROR`、`Traceback` 或 `Exception`。
- 自动监听仍保持关闭，自动监听运行记录数和审批在途运行数均为 `0`。
