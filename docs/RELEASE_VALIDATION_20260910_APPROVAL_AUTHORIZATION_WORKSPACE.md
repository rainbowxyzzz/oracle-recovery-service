# 2026-09-10 审批流自动授权工作区发布验证

## 范围与方式

- 模式：发布验证（模式二）。
- 变更：将审批流自动授权拆分为“授权任务 / 上下文测试 / 运行记录”三个独立子模块；高级配置改为结构化弹窗；运行记录改为“任务 → 运行实例 → 申请与步骤日志”分层展示。
- 兼容边界：保留现有接口地址、请求字段、数据库结构、调度行为和后台执行规则；结构化高级配置继续写回原有 `config` JSON（配置对象）字段，并保留未被界面识别的扩展项。
- 发布方式：仅热更新 `oracle-recovery-api` 容器内 `/app/src/recovery_service/static/ui.html`；未重建镜像、未重启容器、未迁移数据库、未执行审批授权任务。

## 本地验证

- 审批授权定向回归：`9 passed`。
- 主测试集：`350 passed, 1 skipped, 33 subtests passed`。
- `ui.html` 内联脚本的 Node `vm.Script` 语法检查通过。
- 新增测试文件 Ruff 检查通过，`git diff --check` 通过。

## 128 发布前保护

- API 健康检查返回 `status=ok`，MySQL 连接正常。
- 审批授权运行、恢复任务、数据工作流的在途数量均为 `0`。
- 更新前页面备份：`/opt/oracle-recovery-hotfix-backups/approval-auth-workspace-20260910-r1/ui.html`。
- 更新前页面 SHA-256：

  ```text
  aaf6f51770a211ed3015799278fec7c1b03d94d37ef17d218ef08748bf35dd8e
  ```

## 发布结果

- 本地候选、API 容器文件、服务器本机 HTTP 响应和开发机访问 128 的 HTTP 响应 SHA-256 一致：

  ```text
  cec96fc351fc740c9cb29d77f4c95a7398ee4d4eecf6208f1063e4afd0e013c3
  ```

- 在线页面已检出“授权任务 / 上下文测试 / 运行记录”三个工作区标记。
- 统一认证配置仍为启用状态；管理员登录、任务配置列表、运行记录列表接口均返回 HTTP 200。
- 现有 `1` 个任务配置和 `3` 条运行记录可读取，配置响应仍包含原有 `config` JSON（配置对象）。
- API、审批授权 Worker（工作进程）和系统 MySQL 均保持运行，启动时间未变化，证明本次未重启容器。
- 发布后在途任务仍为 `0`；近 10 分钟 API 与审批授权 Worker 日志未发现 `Traceback`（异常堆栈）、`Exception`（异常）或 `Error`（错误）记录。

## 可见页面限制

当前浏览器自动化通道未能附着到已打开的浏览器标签页，因此未把真实鼠标点击、弹窗开合和多尺寸视觉检查标记为通过。服务器更新、在线静态文件一致性、接口兼容性、服务健康和错误日志验证均已完成。
