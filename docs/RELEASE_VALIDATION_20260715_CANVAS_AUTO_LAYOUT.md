# 离线开发画布自动排布发布验证报告

日期：2026-07-15  
验证环境：`192.168.150.128`  
访问入口：`http://192.168.150.128:8000/ui`

## 1. 发布范围

本次采用文件级热更新，修改离线开发静态页面并同步到 `oracle-recovery-api` 和 `oracle-recovery-worker`，未重新打包镜像，未修改数据库结构、调度数据和业务恢复目标库。

主要内容：

- 开发模式新增“自动排布”按钮。
- 按依赖关系进行自上而下的确定性分层布局。
- 支持分支、汇聚、互不相连的流程、孤立节点和环形依赖。
- 根据实际节点边界自动扩展画布宽高，并按实际节点尺寸绘制连线。
- 自动排布后立即保存开发版本坐标；上线模式保持只读。

## 2. 发布保护

- 发布前 Celery `active`、`reserved`、`scheduled` 均为空。
- API、Worker 原始 `ui.html` 已分别备份到 `/root/oracle-recovery-hotfix/canvas-auto-layout-20260715/backup`。
- 本次未执行数据库迁移，未创建或修改业务表。

## 3. 自动化测试

- UI 内联 JavaScript 完整语法解析通过。
- 新增布局测试覆盖按钮权限、即时保存、确定性、菱形分支、环形依赖、孤立节点、坐标不重叠和宽分支画布扩展。
- 本机 Node.js 行为测试通过。
- API 容器完整测试：34项通过，1项因容器没有 Node.js 跳过；跳过项已在本机执行通过。
- Worker 容器完整测试：34项通过，1项因容器没有 Node.js 跳过；跳过项已在本机执行通过。
- 现有内容哈希测试继续证明 `x`、`y` 坐标不影响 `execution_content_hash`。

## 4. 真实页面端到端验证

通过系统 API 创建临时开发流程，8个节点初始全部位于 `(32, 32)`，依赖关系包含：

- 菱形分支与汇聚：`A -> B/C -> D`。
- 环形依赖：`E <-> F`。
- 孤立节点：`G`、`H`。

使用本机 Edge 无头模式登录线上页面，按真实用户路径执行：离线开发 -> 默认文件夹 -> 临时流程 -> 自动排布。

验证结果：

- “自动排布”在开发版本中可见且可点击。
- 保存请求 `PATCH /api/v1/data-platform/versions/{version_id}` 返回200。
- 页面提示“画布已自动排布并保存”。
- API 回读的8个节点坐标与页面一致，坐标全部唯一。
- `A` 位于 `B/C` 上游，`B/C` 位于 `D` 上游，分支层级正确。
- 环形依赖节点和孤立节点均获得有限且不重叠的坐标。
- 画布由固定最小高度扩展到1164px，完整容纳纵向组件。
- 窄屏下工作台切换为单列，画布容器保持横向滚动，按钮、节点、文字和连线无重叠。
- 页面无 JavaScript 运行错误，除浏览器默认请求 `favicon.ico` 外无失败资源；最终严格检查已排除 favicon 并确认无业务请求失败。
- 临时流程在每次验证结束后均已归档，活动流程列表无测试数据残留。

截图：

- `tmp/canvas-auto-layout-desktop.png`
- `tmp/canvas-auto-layout-narrow-canvas.png`

## 5. 发布后服务验证

- API 健康接口 `/api/v1/health` 返回 `status=ok`，MySQL连接成功。
- Worker `inspect ping` 返回 `pong`。
- API、Worker 中 `ui.html` SHA256一致：`3a4c5bfe219aaa72c68d05d4ddeb9442f29f8a23fb0dd7c5c4dcc521eefbe8d8`。
- 发布后 API、Worker 最近日志无新增 `Traceback`、`ERROR` 或 `Exception`。

## 6. 交付说明

本次用户要求新增功能，但未再次要求生成部署包，因此仅完成源码、测试、文档和目标服务器热更新，没有修改现有交付 ZIP 或镜像标签。
