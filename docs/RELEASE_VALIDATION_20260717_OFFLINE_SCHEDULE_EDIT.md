# 离线开发调度设置编辑修复发布验证

日期：2026-07-17  
验证环境：`192.168.150.128`  
访问入口：`http://192.168.150.128:8000/ui`

## 1. 问题与修复

- 修复上线调度页无条件禁用调度周期、执行时间和日期控件的问题。
- 修复必须切换“启用上线调度”复选框后部分输入框才意外恢复的问题。
- 修复保存时只提交 `schedule_enabled`、周期和时间等修改被旧值覆盖的问题。
- 上线版本继续冻结流程节点和连线，但允许独立修改完整调度规则，并立即重算 `next_run_at`。

## 2. 自动化验证

- API 容器内 `test_data_platform_tree.py` 共 5 项通过。
- 新增用例验证上线版本可从每日调度修改为每月 18 日 22:30，同时流程节点保持不变。
- 新增用例继续验证上线版本不能直接修改流程节点。
- API 和 Worker 中 `data_platform.py` SHA256 一致：`0cd643e08a77b62cc0409fd51138095bb3b1e38eb8c43d6862a9cbb74f955f75`。
- API 健康检查正常，Worker `ping` 正常，发布后 15 分钟日志无新增 `Traceback`、`ERROR` 或 `Exception`。

## 3. Chrome 真实页面验证

使用隔离流程完成“创建开发版、提交上线、发布、打开调度设置、修改、保存、刷新回读、归档清理”闭环。

- 未取消“启用上线调度”时，可直接将调度周期从每日改为每月。
- 切换每周时启用时间和星期输入；切换间隔时只启用间隔分钟；切换每日时只启用时间；切换每月时启用时间和日期。
- 保存 `每月 / 21:37 / 23 日 / 已启用` 返回 HTTP 200。
- 页面提示下次执行时间为 `2026/7/23 21:37:00`。
- 刷新并重新打开调度页后，周期、时间、日期和启用状态与保存值一致。
- Chrome 控制台错误和 `pageerror` 均为 0。
- 隔离流程已归档，测试调度已清除。

窗口验证尺寸：

| 状态 | outer | inner | 页面横向溢出 |
|---|---:|---:|---|
| 最大化 | `1920x1032` | `1920x945` | 无 |
| 左半屏 | `960x1032` | `944x937` | 无 |
| 右半屏 | `960x1032` | `944x937` | 无 |
| 还原窗口 | `960x1032` | `944x937` | 无 |

截图：

- `tmp/offline-schedule-edit-maximized.png`
- `tmp/offline-schedule-edit-full-page.png`

## 4. 发布信息

- 热更新备份：`/root/oracle-recovery-hotfix-backups/20260717-154958-offline-schedule-edit-fix`
- 本次未修改数据库结构，未生成完整部署包。
